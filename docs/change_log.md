# 변경 기록

이 문서는 프로젝트 방향, 버전 계획, 문서 구조, 구현 기준이 바뀐 내용을 기록한다.

## 2026-06-10

### v1.4.1 편의 기능 보정

변경 기준:

- 기준 스타일을 타입별 스타일 저장 방식에서 공통 생성 색상 하나로 단순화했다.
- 새 컴포넌트 생성 시 기준 색상을 자동 적용한다.
- `Ctrl+Z`, `Ctrl+Y`로 이전/이후 작업으로 이동할 수 있게 하고 최대 5개까지 기억한다.
- 빈 캔버스 우클릭 메뉴에는 컴포넌트 추가 항목만 표시한다.
- Shift + 클릭은 선택 해제가 아니라 선택 그룹에 추가하는 방식으로 정리했다.
- 선택 그룹에서 우클릭 삭제를 하면 선택된 그룹 전체를 삭제한다.
- 잠금 컴포넌트가 클릭/마우스다운 이벤트를 가로막지 않도록 조정했다.
- 상단 버전 표시와 패키지 버전을 `v1.4.1`로 변경했다.

영향받는 문서:

- `README.md`
- `docs/v1/checklist.md`
- `docs/v1/data_rules.md`

### v1.4.0 구현 완료

변경 기준:

- 왼쪽 사이드바를 제거하고 캔버스 작업 영역을 넓혔다.
- 저장, 불러오기, 코드 복사, 화면 크기 선택을 상단바로 이동했다.
- 빈 캔버스 우클릭 위치에 컴포넌트를 생성할 수 있게 했다.
- 컴포넌트 우클릭 메뉴와 `Ctrl+C`, `Ctrl+V`로 복사/붙여넣기를 할 수 있게 했다.
- Shift + 클릭으로 다중 선택을 추가/해제할 수 있게 했다.
- 컴포넌트 고정 기능과 작업 패널 잠금 관리 탭을 추가했다.
- 작업 패널 기준 스타일 탭을 추가하고 새 컴포넌트 생성 시 기준 스타일을 적용할 수 있게 했다.
- 상단 버전 표시와 패키지 버전을 `v1.4.0`으로 변경했다.

영향받는 문서:

- `README.md`
- `docs/v1/checklist.md`
- `docs/v1/data_rules.md`
- `docs/v1/file_structure.md`

### v1.4 기획안 추가

변경 기준:

- v1.4는 왼쪽 사이드바를 제거하고 캔버스 중심 작업 흐름으로 전환한다.
- 컴포넌트 추가는 캔버스 우클릭 중심으로 처리한다.
- 저장, 불러오기, 코드 복사, 화면 크기 선택은 상단바로 이동한다.
- 프레임/레이어 사이드바와 그룹 관리 UI는 보류한다.
- 컴포넌트 복사/붙여넣기, Shift + 클릭 다중 선택, 컴포넌트 고정 기능을 우선한다.
- 작업 패널에 잠금 관리와 기준 스타일 관리 탭을 추가하는 방향으로 정리했다.

영향받는 문서:

- `docs/v1/v1_4_plan.md`
- `docs/v1/README.md`
- `docs/v1/checklist.md`

### v1.3.2 편집 사용성 개선

변경 기준:

- 텍스트 컴포넌트의 캔버스 표시와 생성 HTML을 가운데 정렬로 맞췄다.
- 텍스트 컴포넌트에서 글자 크기를 조절할 수 있게 했다.
- 숫자 입력 중 최소값 보정 때문에 1의 자리 입력이 고정되는 문제를 줄이기 위해 공통 숫자 입력 필드를 추가했다.
- 입력창 라벨 표시 여부를 선택할 수 있게 했다.
- 꾸미기용 `divBox` 컴포넌트를 추가하고 캔버스, HTML 생성, 저장/불러오기 흐름에 연결했다.
- 상단 버전 표시와 패키지 버전을 `v1.3.2`로 변경했다.

영향받는 문서:

- `README.md`
- `docs/v1/checklist.md`
- `docs/v1/data_rules.md`
- `docs/v1/file_structure.md`

### v1.3.1 버그 수정

변경 기준:

- 컨테이너 안에 생성한 컴포넌트의 드래그가 부모 컨테이너와 충돌하지 않도록 수정했다.
- 작업 패널을 떼었을 때 헤더를 잡고 자유롭게 이동할 수 있게 했다.
- 캔버스 확대/축소 입력을 `Alt + 마우스 휠`에서 마우스 휠 단독 동작으로 변경했다.
- 상단 버전 표시를 `v1.3.1`로 변경했다.

영향받는 문서:

- `docs/v1/checklist.md`

### v1.3 구현 완료

이전 기준:

- v1.3 기획안은 컨테이너 중심 구조, 애니메이션 미리보기, 도킹 작업 패널, 꾸미기 확장을 목표로 했다.
- 오른쪽 패널은 고정 사이드바였고, 스타일 편집은 배경색/글자색/기본 그라데이션/모서리 중심이었다.

변경 기준:

- 박스 추가 기능을 제거하고, 기존 `box` 저장 데이터는 불러오기 시 `container`로 변환한다.
- 컨테이너 안에 컨테이너를 추가할 수 있고, 중첩 구조는 캔버스와 HTML 생성에서 같은 `parentId` 기준으로 처리한다.
- 캔버스에서 애니메이션을 직접 미리볼 수 있게 하고, 진입 재생과 상태 토글을 제공한다.
- 오른쪽 고정 사이드바를 도킹 작업 패널로 전환하고, 조정/꾸미기/애니메이션/HTML 탭으로 분리했다.
- 작업 패널의 위치, 크기, 열린 탭, 도킹 여부는 project JSON이 아닌 workspace settings에 저장한다.
- 꾸미기 탭에서 그라데이션 기준점, 그림자, border, 모서리, 투명도를 조정할 수 있게 했다.
- 생성 HTML에 그라데이션 기준점, 그림자, border, 투명도, 중첩 컨테이너, 애니메이션 클래스가 반영된다.

변경 이유:

- UI 빌더를 단순 배치 도구에서 실제 화면 설계 도구에 가까운 작업 흐름으로 확장하기 위해
- project 데이터와 작업 환경 데이터를 분리해 내보내기/불러오기 결과를 예측 가능하게 유지하기 위해
- 캔버스에서 보이는 결과와 생성 HTML 결과의 차이를 줄이기 위해

영향받는 문서:

- `docs/v1/checklist.md`
- `docs/v1/data_rules.md`
- `docs/v1/file_structure.md`
- `docs/v1/v1_3_plan.md`

### v1.3 기획안 추가

변경 기준:

- v1.3은 컨테이너 중심 구조, 애니메이션 미리보기, 도킹 작업 패널, 꾸미기 기능 확장을 다음 목표로 둔다.
- 기존 `box`는 컨테이너와 역할이 겹치므로 새 UI에서는 제거하고, 저장 데이터에서는 `container`로 변환한다.
- project JSON과 workspace settings를 분리한다.

영향받는 문서:

- `docs/v1/v1_3_plan.md`
- `docs/v1/README.md`
- `docs/v1/checklist.md`

### v0 문서 폴더 정리

변경 기준:

- v0 전용 문서는 `docs/v0/` 아래로 모았다.
- 여러 버전에서 함께 쓰는 `workflow`, `change_log`, `commit_convention`, `pr_convention`은 `docs/` 최상위에 둔다.
- v1 문서는 `docs/v1/`에서 관리한다.

영향받는 문서:

- `docs/README.md`
- `docs/v0/README.md`
- `docs/v0/product_plan.md`
- `docs/v0/development_rules.md`
- `docs/v0/file_structure_rules.md`
- `docs/v0/version_checklist.md`
- `docs/v0/history.md`

### v1 기획 문서 분리

변경 기준:

- v0 문서는 참조 가능한 기준으로 보관하고, 다음 작업은 `docs/v1/`에서 v1 목표와 구조를 다시 기획한다.
- v1 방향은 영역 구성, 반응형 작업 환경, 스타일 확장, 애니메이션, 캔버스 조작 중심으로 둔다.

영향받는 문서:

- `docs/v1/README.md`
- `docs/v1/product_plan.md`
- `docs/v1/workflow.md`
- `docs/v1/checklist.md`
- `docs/v1/file_structure.md`
- `docs/v1/data_rules.md`

## 2026-06-09

### 파일 구조 기준 변경

변경 기준:

- `App.jsx`에 몰려 있던 상태 관리와 화면 조립을 기능별 파일로 분리한다.
- `features/builder/` 중심의 구조를 사용한다.
- `toolbar/`, `canvas/`, `inspector/`, `code/`, `model/`, `codegen/`, `storage/`로 역할을 나눈다.

변경 이유:

- 새 기능을 추가할 때 수정 위치와 책임을 명확하게 유지하기 위해
- 초보자도 파일 이름만 보고 수정 위치를 찾기 쉽게 하기 위해

영향받는 문서:

- `docs/v0/file_structure_rules.md`
- `docs/v0/development_rules.md`
- `docs/v0/product_plan.md`
- `docs/v0/version_checklist.md`
